The 'getReportsByInstructor' function that fetches all reports of all students that are assigned to an instructor
--> We should know if it will be actually needed in the frontend, and if, we will call it distinctly in the instructor module 
(Like createReport() and getReportsByStudent() )